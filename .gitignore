# hiding some folders/files for security

# Local .terraform directories
**/.terraform/

# .tfstate files and backups
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which may contain sensitive data
# (e.g., passwords, private keys, and other secrets)
*.tfvars
*.tfvars.json

# Ignore override files as they are usually for local changes
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# Example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore graph output files generated by `terraform graph`
# *.dot
